import { EmailValidationInterceptor } from './email-validation.interceptor';

describe('EmailValidationInterceptor', () => {
  it('should be defined', () => {
    expect(new EmailValidationInterceptor()).toBeDefined();
  });
});
