import { ExpirationDateValidationInterceptor } from './expiration-date-validation.interceptor';

describe('ExpirationDateValidationInterceptor', () => {
  it('should be defined', () => {
    expect(new ExpirationDateValidationInterceptor()).toBeDefined();
  });
});
